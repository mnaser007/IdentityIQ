<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule language="beanshell" name="Rule36_getIdentities_firstName_startwith_Ann_Pa" type="FieldValue">
  <Source><![CDATA[
  
import sailpoint.api.SailPointContext;
import sailpoint.object.Filter;
import sailpoint.object.QueryOptions;
import sailpoint.object.Identity;
import java.util.List;
import sailpoint.tools.GeneralException;
import sailpoint.tools.Util;


//.................List out all active employee firstName Start with 'Ann' or 'Pa'...........
	
Filter first = Filter.like("firstname", "Ann", Filter.MatchMode.START).or(Filter.like("lastname", "Pa", Filter.MatchMode.START));
Filter active=Filter.eq("inactive", true);
	
		

		Filter finalFilter = Filter.and(first, active);
		QueryOptions qo = new QueryOptions();
		qo.addFilter(finalFilter);
		List<Identity> identities = context.getObjects(Identity.class, qo);

		if (!Util.isEmpty(identities)) {
			for (Identity identity : identities) {
				System.out.println("Employee first name end with 'Ann' or 'Pa:----> " + identity.getFirstname());
			}
		}
	}

 
    
  ]]></Source>
</Rule>
