<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule language="beanshell" name="Rule04_get_Application_Delimited" type="FieldValue">
  <Source><![CDATA[
  
 import java.util.List;

import sailpoint.api.SailPointContext;
import sailpoint.object.Application;
import sailpoint.tools.GeneralException;

 //..............Getting an application which is only DelimitedFile..........................
  
           List<Application> application=context.getObjects(Application.class);
           if(application!=null && application.size()>0) {
        	   for (Application apps:application) {
        		   System.out.println("Applications Names:--->"+apps.getName());
        		   }
        		   System.out.println("Total applications:--->"+application.size());
        	   }
           }
           
           
    
  ]]></Source>
</Rule>
