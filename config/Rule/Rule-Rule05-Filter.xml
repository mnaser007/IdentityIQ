<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule language="beanshell" name="Rule05-Filter" type="FieldValue">
  <Source><![CDATA[
  
import sailpoint.tools.GeneralException;
import sailpoint.object.Identity;
import sailpoint.object.QueryOptions;
import java.util.ArrayList;
import java.util.List;
import sailpoint.api.SailPointContext;
import sailpoint.object.Application;
import sailpoint.object.Filter;


 //..............Getting user data using Filter option with department..........................
   		  
   		  ArrayList identityNames= new ArrayList();
         Filter DptFilter =  Filter.eq("department" , "Information Technology");
		 QueryOptions qo=new QueryOptions();
		 qo.addFilter(DptFilter);
		 
		 List<Identity> identities = context.getObjects(Identity.class, qo);
		 
		 if (identities!=null && identities.size()>0)
		 {
		 for (Identity identity:identities) 
		 {
			 System.out.println("Identity Fullname:-->"+identity.getName());
			 System.out.println("Identity Firstname:--> "+identity.getFirstname());
		 
		 identityNames.add(identity.getName()+identity.getFirstname());
		 }
		 }
		 
		return identityNames;
			
           }
           
    
  ]]></Source>
</Rule>
